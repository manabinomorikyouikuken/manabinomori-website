# manabinomori-website

## Local-only files

The brochure HTML drafts and their source photo are intentionally excluded by
`.gitignore`. They are local print-production files, not public GitHub Pages
assets.

Before publishing a brochure as a web page or PDF, rebuild it as a tracked
public artifact, strip image metadata, and check mobile display.
